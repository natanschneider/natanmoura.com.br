services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tanstack-app
    environment:
      - NODE_ENV=production
      - PORT=3000
    expose:
      - 3000
    restart: unless-stopped
    networks:
      - tanstack-network

networks:
  tanstack-network:
    driver: bridge
